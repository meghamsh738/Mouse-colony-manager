# Role-Scoped Colony Platform Implementation Tracker

Last updated: 2026-07-18

This is the source of truth for the role-scoped redesign in the `codex/empty-colony` worktree. A requirement is complete only when its status is **Verified** and evidence is recorded below.

## Status Rules

- **Not started**: no implementation work has begun.
- **In progress**: implementation or verification is active.
- **Blocked**: progress requires an external decision or state change; record the blocker.
- **Implemented**: code exists, but the complete acceptance gate has not passed.
- **Verified**: implementation, tests, review, and required browser/database evidence pass.

## Current Baseline

| Item | State | Evidence / note |
| --- | --- | --- |
| Active Codex goal | In progress | Complete role-scoped redesign, empty instance first |
| Target worktree | Verified | `.runtime-data/worktrees/empty-colony`, branch `codex/empty-colony` |
| Existing dirty work preserved | Verified | No reset, checkout, or unrelated reversion performed |
| Architecture reviews 1-4 | Verified | Roles/privacy, workflows, data/migration, role-specific UI |
| Canonical role foundation | Implemented | `it_head`, `facility_admin`, `cmu_staff`, `lab_user` plus migration compatibility values |
| Capability navigation | Implemented | Central capability and navigation registries; page guards started |
| Empty profiles | Implemented | IT, Facility Admin, CMU, Lab 1, Lab 2; production switch blocked |
| Foundation checks | Verified | Prisma validate, TypeScript, lint, 17 focused tests, build, diff check |
| Independent foundation review | Verified | Milestone 0 independent re-review returned with no blockers |

## Milestone 0: Baseline And Recovery

**Milestone status: Verified**

| ID | Requirement | Status | Acceptance evidence |
| --- | --- | --- | --- |
| M0-01 | Inventory pages, actions, APIs, exports, files, jobs, print views, and Workbook | Verified | `AUTHORIZATION_MANIFEST.md` |
| M0-02 | Record migration files and checksums without rewriting them | Verified | Migrations `0001`-`0005`; checksums captured in milestone ledger |
| M0-03 | Make migration history recreate the canonical empty schema | Verified | Reviewed guard passed fresh migrate, empty seed, migration status, and zero diff |
| M0-04 | Keep `db push` only for disposable databases | Verified | Alias removed; explicit local disposable guard and retained-workflow checker pass |
| M0-05 | Restore-and-test backup procedure | Implemented | `docs/DATABASE_RECOVERY.md`; execution remains a Milestone 9 gate |
| M0-06 | Record schema/database/application versions | Verified | Node 26.2.0, Prisma 6.19.3, TypeScript 5.9.3, PostgreSQL 17.5, Next.js 16.2.2 |

## Milestone 1: Identity And Authorization

**Milestone status: Verified**

| ID | Requirement | Status | Acceptance evidence |
| --- | --- | --- | --- |
| M1-01 | Canonical global roles with legacy migration mapping | Verified | Capability and migration-chain tests pass |
| M1-02 | Active Lab User membership determines effective lab authority | Verified | Session and capability tests derive authority from current active membership |
| M1-03 | IT and CMU memberships never broaden authority | Verified | IT technical-only and CMU private-experiment denial tests pass |
| M1-04 | Database-revalidated actor on every protected entry point | Verified | Pages, API methods, and server actions use current actor plus explicit capability; 70-entry manifest passes |
| M1-05 | Authorization version and immediate session revocation | Verified | Role and active-state changes increment `authzVersion`; stale/deactivated session tests pass |
| M1-06 | Invitation activation with hashed, expiring, single-use tokens | Verified | Database test verifies hash-only storage, lab membership, expiry eligibility, and replay denial |
| M1-07 | Two-person/offline governance for IT/Facility Admin assignment | Verified | Independent approver, self-approval denial, pending uniqueness, and final IT protection tested; production recovery procedure remains rollout prerequisite |
| M1-08 | Explicit active-lab context; no Lab User all-lab mutation mode | Verified | Cookie context is validated against current active memberships; role-interface browser coverage remains M8 |
| M1-09 | Capability-generated navigation and route protection | Verified | Generated navigation, page guards, method-specific API guards, action guards, and CI manifest |
| M1-10 | Development profile switcher impossible in production | Verified | Unit test and server/render guards |

## Milestone 2: Lab Isolation

**Milestone status: Verified**

| ID | Requirement | Status | Acceptance evidence / open work |
| --- | --- | --- | --- |
| M2-01 | Add explicit lab ownership to every lab-owned entity | Verified | Required ownership fields and fail-closed backfill in migration `0008`; migration replay passes; populated-data rehearsal remains M9 |
| M2-02 | Scope dashboard, counts, badges, metrics, and approvals | Verified | Dashboard, notification inbox, generated alerts, and shell badge use active-lab filters |
| M2-03 | Scope list/detail reads and nested relations | Verified | Animals, cages, breeding, experiments, forecast, quarantine, biosamples, and cryostorage use canonical ownership fields |
| M2-04 | Scope writes and reject submitted foreign-lab IDs | Verified | Mixed-membership adversarial tests cover inventory, project, experiment, lifecycle, cage, animal, genotype import, and transferred cohort paths |
| M2-05 | Scope APIs and direct-ID routes | Verified | All scientific APIs receive the database actor; foreign direct IDs resolve as filtered/404; scan lookup is guarded |
| M2-06 | Scope CSV exports, print views, labels, files, and downloads | Verified | CSV builders, labels, invoice print, and nested attachment metadata use actor/canonical lab scope |
| M2-07 | Scope search, autocomplete, selectors, facets, and pagination totals | Verified | Page selectors/read models and deterministic API identity resolvers retain active-lab scope |
| M2-08 | Make Workbook reuse authorized repositories and projections | Verified | Workbook read model receives actor/lab context; current and historical leak tests pass |
| M2-09 | Add same-lab composite constraints where required | Verified | Migration `0009` adds deferred composite FKs, fail-closed historical-link triggers, active-relation transfer guards, and mismatch assertions |
| M2-10 | Pass two-lab negative isolation suite | Verified | Safe negative suite and retained database verifiers pass; independent P1 re-review returned clear |

## Milestone 3: Commands, Events, And Identity Sequences

**Milestone status: Verified**

| ID | Requirement | Status |
| --- | --- | --- |
| M3-01 | Workflow drafts and immutable review snapshots | Verified |
| M3-02 | Durable idempotent command receipts | Verified |
| M3-03 | Aggregate versions and structured stale conflicts | Verified |
| M3-04 | Transactional outbox, attempts, leases, retries, and dead letters | Verified |
| M3-05 | Topic-specific worker authority and delegated-job reauthorization | Verified |
| M3-06 | Facility animal IDs `0001`-`9999`, immutable and non-reusing | Verified |
| M3-07 | Facility cage IDs `1000`-`9999`, immutable and non-reusing | Verified |
| M3-08 | Legacy aliases, deterministic collision mapping, exhaustion errors | Verified |
| M3-09 | Migration runs and fail-closed ownership exceptions | Verified |

## Milestone 4: Routine Colony Wizards

| ID | Requirement | Status |
| --- | --- | --- |
| M4-01 | Cage creation wizard | Verified |
| M4-02 | Purchased/manual/CSV intake wizard | Verified |
| M4-03 | Deterministic cage planner with review-only proposals | Verified |
| M4-04 | Animal movement and scan wizard | Verified |
| M4-05 | Litter logging and multi-cage weaning wizard | Verified |
| M4-06 | Missing/found wizard | Verified |
| M4-07 | Save/resume, conflict recovery, receipts, and batch validation | Verified |
| M4-08 | Capacity 6, recommended 5, lower overrides, quarantine and sex rules | Verified |

## Milestone 5: High-Impact Colony Workflows

| ID | Requirement | Status |
| --- | --- | --- |
| M5-01 | Breeding state machine and wizard | Verified |
| M5-02 | Euthanasia/death/external-transfer lifecycle wizard | Verified |
| M5-03 | Quarantine admission, observation, exception, and release | Verified |
| M5-04 | Explicit cage closure and immutable billing cutoff | Verified |
| M5-05 | Source request, destination approval, CMU finalization transfer | Verified |
| M5-06 | Transfer packet privacy and packet-version reapproval | Verified |
| M5-07 | Active experiment/breeding blocks and audited Facility override | Verified |

## Milestone 6: Research And Facility Services

| ID | Requirement | Status |
| --- | --- | --- |
| M6-01 | Full lab Experiments workspace | Verified |
| M6-02 | CMU operational Procedures workspace and DTO | Implemented; browser acceptance pending |
| M6-03 | Lab/user/cage-scoped Forecast | Implemented; browser acceptance pending |
| M6-04 | Lab-owned Biosamples with optional experiment linkage | Verified |
| M6-05 | Lab cryostorage request plus CMU execution | Implemented; browser acceptance pending |
| M6-06 | Immutable facility/lab SOP versions, approvals, assignment, acknowledgement | Implemented; browser acceptance pending |
| M6-07 | Historical procedure/lifecycle references to exact SOP version | Implemented; browser acceptance pending |

## Milestone 7: Notifications, Billing, Audit, Administration

| ID | Requirement | Status |
| --- | --- | --- |
| M7-01 | Events, audiences, materialized recipients, read/ack/resolve | Verified |
| M7-02 | Urgent email, routine digests, preferences, delivery history | Verified |
| M7-03 | Read-time notification reauthorization and revocation cleanup | Verified |
| M7-04 | Cage billing enrollment and immutable charge periods | Verified |
| M7-05 | Draft/finalized/void invoices and immutable adjustments | Verified |
| M7-06 | Lab-visible cage IDs, periods, rates, totals, and invoices | Verified |
| M7-07 | Separate domain audit and technical security events | Verified |
| M7-08 | Append-only database protections and role-projected history | Verified |
| M7-09 | User invitation, lab administration, approvals, technical queue visibility | Verified |

## Milestone 8: Role-Specific Interface

| ID | Requirement | Status |
| --- | --- | --- |
| M8-01 | Persona dashboards and ordered work queues | Verified |
| M8-02 | Consolidated Approvals queue separate from Notifications | Verified |
| M8-03 | Desktop worksheets and mobile row cards | Verified |
| M8-04 | Full-page wizard shell and exact high-impact labels | Verified |
| M8-05 | Read-only Workbook using shared authorized repositories | Verified |
| M8-06 | Empty onboarding for first lab, users, rules, cages, and intake | Verified |
| M8-07 | Zero clipping, page overflow, unauthorized DOM fields, or inaccessible controls | Verified |

M8-01 evidence: actor-scoped Facility Admin, CMU Staff, and Lab User dashboards now expose capability-valid commands and ordered queues. Focused tests cover lab scoping plus manager/staff/viewer approval links; typecheck and ESLint pass. Browser QA covered all three personas and a 390x844 Lab User viewport with zero document overflow and 44px dashboard actions. Independent re-review found no P0/P1 issues. The later M8-06 gate verifies the complete empty-instance onboarding path.

M8-02 evidence: `/approvals` now presents one actor-scoped actionable inbox for privileged access, destination/finalization transfers, quarantine release, cryostorage processing, and independent SOP decisions, while existing domain workflows retain their guarded mutations. Dashboard and page counts use the same action-required semantics. Migration `0035_independent_lab_sop_approval` closes lab SOP self-approval in PostgreSQL; the inbox, SOP read model, and command layer enforce the same maker-checker rule. The disposable r66 schema reports all 35 migrations applied. Browser QA covered Facility Admin, CMU Staff, and Lab Manager access plus 390x844 overflow/touch sizing. Full unit verification passed 85 files / 418 tests, typecheck, production build (50 routes), focused ESLint, and independent security re-review with no P0/P1/P2. Tenant policy rejected the data-writing SOP integration rerun, so that rerun is not claimed.

M8-03 evidence: the remaining role-specific holdouts now use desktop worksheets and phone row cards. Users and invitations use a compact action tray plus responsive lists, expanded lab memberships switch from a desktop table to complete mobile cards, and IT migration/security history preserves its complete audit context on phones. Browser QA at 390x844 found zero page overflow, 44px controls, no visible desktop tables, and an open membership panel contained within its card; 1440x1000 restored desktop tables with zero page overflow. Full unit verification passed 85 files / 420 tests, with typecheck, focused ESLint, `git diff --check`, and independent re-review clear of P0/P1/P2.

M8-04 evidence: shared workflow steps and impact summaries now give intake, lifecycle, cage closure, quarantine release, billing decisions, cryostorage processing, and experiment cohort actions a consistent details-to-review progression. Terminal animal disposition, permanent cage closure, quarantine release, invoice finalize/void, and cryostorage processing open in actor-scoped action-only pages using the shared high-impact shell; the scan page routes cage closure to the canonical cage workflow. Exact final actions name the record or outcome, while quarantine capacity/reason/date, cage billing cutoff, invoice locked/removed total, cryostorage inventory status, intake charging, and experiment reservation effects are visible before submission. The release UI aggregates proposed destination occupancy before review, and actor-scoped completion reads keep post-submit states stable without broadening visibility. Browser QA on the empty instance covered the three-step cage-intake review at desktop and 390x844, with zero overflow and no sub-44px workflow controls; Cryostorage, Quarantine, and Experiments also had zero mobile overflow. Final full unit verification passed 87 files / 425 tests, typecheck and production build (50 routes) passed, lint completed with only the five pre-existing administration action warnings, and `git diff --check` passed. Independent initial review findings on landmarks, accessible destination labels, exact completion states, and aggregate capacity were remediated; re-review returned CLEAR with no P0/P1/P2.

M8-05 evidence: Workbook reads now compose the actor-aware animal, cage, breeding, experiment, biosample, cryostorage, alert, and lab-scope repositories instead of querying Prisma independently. Foreign-owner cage occupants and their derived aggregates, wrong-lab alerts, project allocations, experiment linkage, history records, labs, sheets, and child identities fail closed through the shared projections. The Workbook remains read-only and URL-backed; invalid, inaccessible, whitespace-padded, duplicate, and explicit-default query state canonicalizes to an authorized URL. Mobile uses a 44 px sheet selector and expandable cards without page overflow, while desktop retains sticky worksheet navigation. Browser QA covered Facility Admin, CMU Staff, and Lab User 1 permissions, forged URL state, desktop and 390x844 layouts, and zero document overflow. Full unit verification passed 89 files / 432 tests; typecheck, production build (50 routes), focused ESLint, and `git diff --check` passed. The protected destructive database integration rerun was correctly refused without an approved disposable test URL and is not claimed. Independent final re-review returned CLEAR with no P0/P1/P2 findings.

M8-06 evidence: the empty-instance Facility Admin onboarding checklist covers creation of the first active lab, active lab-user membership, facility rules, cages, and animal intake without treating IT, CMU, inactive identities, inactive labs, or inactive memberships as completed lab-user setup. The rule editor's Close/Cancel control returns cleanly from its expanded state. Focused verification passed 2 files / 6 tests, typecheck, ESLint, and `git diff --check`; independent re-review returned CLEAR with no blocking finding.

M8-07 evidence: mobile touch targets have been tightened across dashboard, rule editor, intake, animals, cages, labels, scan, and billing; dense cage-detail tables are suppressed in the phone presentation. Detail and scan projections fail closed for manual alerts, health-note attachments, occupants, derived capacity, and rule alerts by using the cage lab as their canonical scope. Mobile cage cards retain lab, capacity, rate, sex mix, project, strain, animal identifiers, warnings, and touch-sized actions without the desktop dense-details disclosure. On Node 26, Turbopack spun up unbounded workers while compiling `/login`; `npm run dev` therefore uses the verified webpack mode. The guarded empty bootstrap ran against loopback-only PostgreSQL 16 schema `mcm_test_administration_20260717_r66`; all 35 migrations applied and readback confirmed five users, two labs, two memberships, zero cages, and zero animals. Authenticated desktop QA covered Facility Admin administration, settings, onboarding, colony, scan, labels, billing, and approval screens with zero document overflow. Authorization checks confirmed IT Head access to `/system` with lab administration denied and absent from the denied DOM, Facility Admin denial from `/system`, Lab User 1 colony access with facility administration denied and absent from the denied DOM, and CMU Staff approvals access without administration navigation or fields. At 390 x 844, Facility Admin, CMU Staff, Lab User 1, and IT Head representative workspaces had zero document overflow, no visible desktop tables in the lab-user colony presentation, and no sub-44 px effective controls. Authenticated QA found one 33 px-wide billing Rates tab; adding a 44 px minimum width corrected it, and the focused retest measured all billing tabs at least 44 x 44 px with zero overflow. The login screen also passed desktop and 390 x 844 QA without framework errors. Final full verification before authenticated acceptance passed 90 files / 434 unit tests, typecheck, production build (50 routes), authorization-manifest validation (80 protected entry points), Prisma validation, and `git diff --check`; lint had only the five pre-existing unused-argument warnings in administration actions. Independent M8-07 review returned APPROVED with no blockers, and the final focused diff review after the billing correction returned CLEAR.

## Milestone 9: Populated Rehearsal

| ID | Requirement | Status |
| --- | --- | --- |
| M9-01 | Production-shaped backup restore | Not started |
| M9-02 | Expand/backfill/verify/constrain/cutover/contract rehearsal | Not started |
| M9-03 | Ownership and legacy-state exception resolution | Not started |
| M9-04 | Revoke legacy sessions at cutover | Not started |
| M9-05 | Row-count, constraint, rollback, and roll-forward evidence | Not started |

## Milestone 10: Performance And Hosting

| ID | Requirement | Status |
| --- | --- | --- |
| M10-01 | Pagination and bounded relationship history | Not started |
| M10-02 | Narrow projections and request-scoped actor/access context | Not started |
| M10-03 | Loading boundaries and measured indexes | Not started |
| M10-04 | Bounded outbox workers and queue observability | Not started |
| M10-05 | 10k animals, 2k cages, 50-user stress run | Not started |
| M10-06 | Record p50/p95, query count, memory, pool failures, queue delay, and error rate | Not started |
| M10-07 | Hosting decision based on measured evidence | Not started |

## Required Gate For Every Milestone

- [ ] Focused unit and integration tests pass.
- [ ] `npm run prisma:validate` passes when schema is affected.
- [ ] `npm run typecheck` passes.
- [ ] `npm run lint` passes.
- [ ] `npm run build` passes.
- [ ] `git diff --check` passes.
- [ ] Required database migration/recovery tests pass.
- [ ] Required desktop/mobile browser and accessibility checks pass.
- [ ] Independent bounded review has no blocking findings, or reviewer unavailability and manual review are recorded.
- [ ] Tracker statuses and evidence are updated.

## Milestone Ledger

### 2026-07-16 — Procedure Operations And Exact Lifecycle SOP Provenance

- Added lab-authored, exact-SOP-assignment-bound procedure plans and append-only CMU/Facility procedure occurrences. Plans freeze the reviewed assignment and operational experiment context; execution rejects material context drift and occurrences retain the same immutable context plus animal, cage, location, experiment, and SOP provenance.
- Replaced the Procedures placeholder with an operational-only workspace, guarded server actions, and capability-projected API endpoints. Private experiment notes/results, assignment notes, and SOP content are not selected by the procedure read model.
- Added planned procedures as non-overridable lab-transfer blockers. A plan must be cancelled through its own audited command before ownership can change; a database trigger independently enforces the same invariant.
- Required euthanasia lifecycle review and the external animal lifecycle API to select the current approved SOP assignment for the animal's lab. The append-only lifecycle event and audit payload retain the exact assignment ID, SOP ID, version ID, version number, and content hash; lifecycle and procedure timestamps cannot predate version creation, approval, assignment, or plan creation. Database-wide append-only audit protection remains M7.
- Fresh disposable schema `mcm_test_procedure_20260716_r20` replayed migrations `0001` through `0019`; read-only PostgreSQL catalog inspection confirmed all frozen-context columns, exact composite SOP-assignment foreign keys, seven enabled procedure/lifecycle guards, four context/provenance functions, and full-timestamp lifecycle chronology without date casts. Prisma's cross-named-schema diff remained namespace-noisy and was not used as evidence.
- Passed Prisma format/validate/generate, typecheck, lint, production build, 77-entry authorization-manifest validation, `git diff --check`, 46 focused procedure/SOP/seed tests, and 55 unit files / 261 tests. The disposable reseed order now removes procedure occurrences, plans, and lifecycle events before restricted SOP parents. The environment blocked the standalone data-writing migration-chain verifier, so no seed or data-writing database test was claimed. The retained production smoke still covers unauthenticated route and API boundaries; role-specific browser acceptance remains pending.
- Independent high-risk re-review passed with no P0/P1/P2 finding. Role-specific desktop/mobile browser acceptance remains required before M6-02 and M6-07 become Verified; tenant-blocked data-writing database cases remain a documented P3 verification gap rather than inferred evidence.

### 2026-07-12 — Foundation Slice

- Added canonical roles, `authzVersion`, actor resolution, active-lab context, capabilities, generated navigation, page guards, IT System, CMU Procedures, Approvals, and five empty profiles.
- Added migration `0004_role_foundation`.
- Fixed profile switching so it cannot render or execute in production.
- Passed Prisma validation, typecheck, lint, 17 focused tests, production build, and diff check.
- Review agents were attempted twice but did not return. Manual review found and fixed the production profile-switch exposure.
- Migration checksums at baseline:
  - `0001_init`: `b391aa5578d26ca044f27c292c23bd2d5d215e5deece0edbfa32b6828c6368ee`
  - `0002_sample_records`: `82243e39a84f730ac4a4a51115e4aefd592d6f9dc5077af529edd50a952b63cf`
  - `0003_cryostorage_records`: `1ce73d268530b1a5da6b7a69a7c171b45132cde5e8b05da598b2b29e18f26d2d`
  - `0004_role_foundation`: `c434189ecd4a022595e6d885dc17bf42b9a6364e72a217aa1c155ce11f4705e3`
  - `0005_schema_reconciliation`: `9dba50b890ec0b97172b4f0708bdc7d7d490344d6d84dd32a3901d77e3cacfad`

### 2026-07-12 — Migration Reconciliation

- Added `migration_lock.toml` and generated `0005_schema_reconciliation` from migrations `0001`-`0004` to the canonical Prisma schema.
- Applied all five migrations to a separate fresh Prisma PostgreSQL instance on port `51222`.
- `prisma migrate diff` reported no difference between the migrated database and `schema.prisma`.
- Empty bootstrap seed completed with all operational tables empty.
- The previous configured Prisma-dev database matched migrations `0001`-`0004` but returned protocol errors during migration metadata baselining; the empty worktree now points to the fresh verified instance instead of forcing metadata changes into the unstable database.
- Added retained-data workflow enforcement and documented the mandatory populated backup/restore rehearsal. PostgreSQL client backup tools are not currently installed and remain a Milestone 9 prerequisite.

### 2026-07-12 — Milestone 0 Review Findings

- Reviewer blocked the first gate because reconciliation could be applied to populated data, `db:push` retained an unguarded alias, and fresh-schema parity was not automated.
- Reconciliation now aborts if users, facilities, animals, or cages already exist. Populated databases must use the later expand/backfill rehearsal and may mark this baseline applied only after a zero-diff proof.
- Removed the `db:push` alias. Disposable push requires an explicit flag, a loopback database, and a non-production process.
- Added `check:migration-chain` to the full verification command.
- Reverified on a fresh Prisma PostgreSQL instance: all migrations applied, empty bootstrap passed, migration status was current, and schema diff was empty.
- Verified the populated guard independently: a database with migrations `0001`-`0004` plus one synthetic user rejected `0005` with its explicit fail-closed error.
- Automated verification now replays migrations and the empty seed in a uniquely named retained verification schema without deleting database objects.
- Verified the disposable-push guard refuses the configured empty instance because retained configuration and users exist.
- Final independent re-review: no remaining Milestone 0 blockers.

### 2026-07-12 — Identity And Authorization Gate

- Added database-revalidated actors, immediate `authzVersion` revocation, hashed expiring single-use invitations, public activation, Facility Admin user administration, routine access changes, account activation/deactivation, and two-person privileged-role approval.
- Added method-specific API capabilities and mutation-specific server-action capabilities. A machine-checked manifest now covers 70 pages, route handlers, and server-action modules.
- Split experiment read access from `experiments:manage`; lab viewers can inspect authorized experiment records but cannot mutate assignments, reservations, or projects.
- Added migrations `0006_identity_governance` and `0007_identity_governance_constraints`; a fresh seven-migration replay, schema parity check, and empty bootstrap passed.
- Migration checksums:
  - `0006_identity_governance`: `c3687c8bc91db7da12ce036d5cd05b5c4f96b46333b766033621dc2f116b2f7a`
  - `0007_identity_governance_constraints`: `a554dbbf2848c7f8b15cf043a350a6723c71bc5542f4b8b94569201cca035d56`
- Verification passed: Prisma validation, TypeScript, ESLint, authorization manifest, `git diff --check`, production build, 6 database-backed identity tests, and 170 remaining unit/integration tests (176 total).
- The Prisma-dev proxy becomes unreliable when database-heavy identity tests run after the 62-reset API suite. The test command now runs the self-contained identity project first, then the remaining suite in a separate process; both pass on one fresh migrated database.
- Independent security review is in progress; Milestone 1 remains unverified until that review has no blocking findings.

### 2026-07-12 — Identity Review Remediation And Lab Isolation Start

- Independent review identified three blocking risks: mixed-membership active-lab confusion, unguarded destructive demo seeding, and no safe adoption path for populated databases stalled before reconciliation migration `0005`.
- Governance mutations now re-read actor active state, canonical role, and `authzVersion` inside their transaction, preventing an already-revoked detached actor snapshot from completing an in-flight privileged change.
- Destructive fixture seeding is now explicit, non-production, loopback-only, and restricted to database/schema names containing a disposable marker. Normal local/CI preparation migrates without deleting records.
- Added a zero-diff populated-baseline adoption workflow requiring retained verification evidence, a backup identifier, and an exact operator confirmation before recording `0005` and deploying later migrations.
- Added migration `0008_explicit_lab_ownership`; a disposable schema successfully replayed migrations `0001` through `0008`. The migration fails closed when required ownership is ambiguous.
- Active-lab scoping has landed for dashboard, badges, notifications, animals, cages, breeding, experiments, forecast, quarantine, biosamples, cryostorage, selectors, and Workbook repositories.
- API/export scoping, cross-lab mutation tests, demo-seed compilation, the full verification command, and independent Milestone 1/2 re-review remain open. No populated data has been changed.
- API, CSV export, scan lookup, Workbook, cage-label, dashboard, notification, inventory, forecasting, quarantine, and billing boundaries now receive the database-resolved actor and filter on canonical ownership fields.
- Added adversarial mixed-membership tests for a user who manages Lab A and views Lab B. Direct foreign sample, project, cryostorage, assignment, cage, animal, lifecycle, barcode, invoice, Workbook, and genotype-import paths are rejected or filtered.
- Verification evidence after integration: TypeScript passed; ESLint passed; production build passed; Prisma schema validation passed; authorization manifest covered 70 entry points and now checks declared source guards; 33 focused isolation tests passed; the remaining safe unit subset reported 77 passing tests.
- Fresh retained migration verification replayed all eight migrations, reported zero schema diff, bootstrapped the empty instance, and left operational counts at zero. Migration `0008` checksum: `cf827462d54b4e7f3fbb819cbfb846a8da2e32ca5ab69ab33989c2073c2894ed`.
- The database-backed fixture suite is not yet rerun because it deletes and reseeds synthetic rows; tool policy requires explicit user approval even for the dedicated `mcm_test_full` schema. This is an open verification item, not a code bypass.
- Independent Milestone 1/2 security re-review is active; neither milestone is marked Verified until it returns without P0/P1 findings and the allowed database gate is complete.

### 2026-07-12 — Milestone 1/2 Security Review Remediation

- First independent re-review returned NOT CLEAR with three P1 blockers: CMU private experiment disclosure through Workbook/animal detail, globally unscoped project/experiment options for lab users, and absent enforceable same-lab relational integrity.
- CMU is now denied the Workbook experiment section and receives no experiment navigation, overview metric, active-work row, assignment, timeline event, or experiment option. Lab users receive only active-lab project and experiment options.
- Read models now fail closed on inconsistent sample/animal/project, cryostorage/project, health-note/cage/animal, attachment, genotype, assignment, current-cage, and dashboard relations.
- Migration `0009_same_lab_integrity` adds deferred composite foreign keys for current-state relations and validated constraint triggers for historical/hybrid relations. Open assignments, allocations, and current breeding participation block ownership changes that would strand a cross-lab relation.
- A retained synthetic mismatch verifier applies migrations `0001`-`0008`, creates a Lab A Experiment linked to a Lab B Project, and proves migration `0009` rejects it. The normal nine-migration replay, schema parity, and empty bootstrap also pass.
- Additional write fixes reject a foreign-lab project during animal creation, ignore transferred animals during cohort promotion/demotion, scope genotype CSV lookup, and authorize direct IDs before returning state-specific errors.
- Empty bootstrap now requires non-production, loopback, and an `empty`/`test`/`e2e`/`disposable` database marker, and refuses any pre-existing identity/configuration or operational records.
- Second independent re-review returned NOT CLEAR with three P1 findings: bootstrap validation could inspect a different URL from the one Prisma writes, migration preflight treated valid ended provenance as current inconsistency, and historical Workbook relations could expose an animal's current private state after a lab transfer.
- Empty bootstrap now validates `DATABASE_URL` as the write target, validates `DIRECT_DATABASE_URL` separately when present, and requires both URLs to identify the same database/schema. Destructive bootstrap URL tests cover mismatched database and schema targets.
- Migration `0009` now blocks only open experiment assignments, open project allocations, and current breeding participation while retaining completed/cancelled assignments, ended allocations, retired/failed breeding adults, and intake-batch provenance. A retained non-destructive verifier proves all four allowed history classes survive the migration, while the mismatch verifier still proves an invalid current Experiment/Project relation is rejected.
- Workbook breeding adults, litter progeny, and experiment assignments are filtered by each animal's current owning lab for lab-scoped users. Historical setup/experiment rows remain visible, but transferred-animal IDs, genotype, cage, and status are omitted when no immutable snapshot exists. Sample selectors now use `Animal.owningLabId`, not the legacy lab-animal identifier field.
- Integrated verification: Prisma validation, TypeScript, ESLint, 70-entry source-guard manifest, production build, 83 safe unit tests, fresh nine-migration replay, zero schema diff, guarded empty bootstrap, mismatch rejection, and retained-history evidence all pass. Migration `0009` checksum: `eedd6f442901bdbedd4dc0c65363e94485dd01bebbd7fdbb55b567dbf053017a`.
- A third independent P1 security re-review is pending. Database fixture tests remain an explicit-approval gate because they bulk-delete and reseed only the disposable synthetic schema.
- Third review returned NOT CLEAR with three P1 disclosures: CMU could reconstruct private experiment codes in the animals API, destination labs could receive source-lab health-note/alert text after an animal transfer, and Workbook welfare projections scoped historical notes through the linked entity's current owner.
- The animals API now skips the experiment-assignment query entirely unless the actor has `experiments:full`, and still filters returned assignments to the animal's current owning lab. A fail-closed regression proves CMU receives no experiment codes.
- Animal list/detail reads now scope health notes, genotype records, and manual alerts directly on each child record's `labId`, then repeat the filter in memory before building warnings or timelines. Actor arguments are mandatory for all animal reads. Transfer-history regressions deliberately return foreign child rows from mocked Prisma calls and prove they are discarded.
- Workbook overview and room welfare reads now filter directly on `HealthNote.labId`. Source-lab biosamples remain visible to their owning lab as historical provenance without exposing the animal's current cage, genotype, or health state.
- Empty bootstrap count-and-create now runs atomically in a serializable transaction with one precomputed password hash. Fresh replay passes, and a second invocation refuses the retained configuration without modifying it. IPv6 loopback guard handling now accepts Node's bracketed hostname representation.
- Post-remediation verification: 91 safe tests, TypeScript, ESLint, `git diff --check`, production build, fresh nine-migration replay, zero schema drift, transactional empty bootstrap, and second-run refusal all pass. Narrow P1 re-review is pending.
- Narrow independent re-review returned CLEAR: all three P1 disclosure paths are closed, its six targeted privacy tests pass, and it found no patch-caused P2. Milestones 1-2 remain at the database fixture gate only; no P0/P1 review blocker remains.
- The full fixture-backed suite was prepared on a fresh local `mcm_test_full` schema with all nine migrations applied, but execution was policy-refused because the suite bulk-deletes and reseeds synthetic rows. It requires the user's explicit approval after this risk disclosure; the disposable server was shut down without running the destructive suite.
- Retained migration verification commands now fail before schema creation on production or non-loopback database targets and run through `tsx`; their guard tests pass.

### 2026-07-12 — Command, Outbox, And Facility Identity Foundation

- Added migration `0010_command_identity_foundation` with workflow drafts, immutable review snapshots, idempotent command receipts, versioned aggregates, transactional outbox records and delivery attempts, facility ID sequences and assignments, legacy aliases, migration runs, and ownership exceptions.
- Command receipt creation, authorization revalidation, stale-version checking, domain writes, outbox enqueueing, and receipt completion now commit in one serializable transaction. A matching concurrent or repeated request replays the committed receipt; mismatched payload reuse returns an idempotency conflict.
- Facility animal IDs allocate from `0001` through `9999`; cage IDs allocate from `1000` through `9999`. Database triggers require prior reservation, record append-only assignments, reject canonical ID mutation, and preserve colony IDs, lab IDs, and cage barcodes as legacy aliases.
- Existing-record backfill is deterministic. Colony animal IDs take precedence when retained colony and lab identifiers collide; direct API resolution has explicit internal/canonical/colony/lab/alias precedence and always retains lab scope.
- Added topic-specific outbox authority, worker leases, retry/dead-letter state, delivery attempts, and current database authorization revalidation before delegated delivery.
- The retained PostgreSQL verifier proves allocation, aliases, version bumps, immutable IDs/snapshots, idempotent replay/conflict, stale conflicts, outbox delivery/dead-letter behavior, delegated authorization, ownership exception resolution, and structured sequence exhaustion.
- Verification evidence after P1 remediation: fresh ten-migration replay, zero schema diff, empty bootstrap, direct retained identity-migration checks, Prisma validation, TypeScript, ESLint, 70-entry authorization manifest, database workflow guard, 116 safe regression tests, production build, and `git diff --check` pass.
- Migration `0010` checksum: `6fca98e6134932d69ff81e47c6d623a866012ad5d9ef3573f74915a9b3c1c726`.
- First independent high-risk review returned NOT CLEAR with eight P1 blockers. The second review closed seven and retained one reciprocal numeric alias/canonical assignment blocker. The migration now enforces both collision directions for animals and cages, and direct PostgreSQL tests cover alias-first and assignment-first transactions. Global ownership-exception recording also reauthorizes Facility Admin against a running migration, and worker token comparison handles unequal UTF-8 byte lengths. A third narrow review is pending; no Milestone 4 work begins until it is clear.
- Third independent review returned CLEAR with no remaining P0/P1 and no residual P2/P3 in the final collision patch. Milestone 3 is Verified and Milestone 4 may begin.

### Milestone 4 evidence (Verified)

- Cage creation, purchased/manual/CSV intake, and multi-cage weaning now execute through one idempotent command boundary in the same serializable transaction as their domain writes.
- Cage intake supports full-state draft save/resume, actor-scoped resume URLs, immutable review snapshots, snapshot-derived idempotency, exact snapshot/draft reservation inside the command transaction, atomic committed draft state, and expected-version protection for litter weaning.
- The deterministic planner groups by sex and strain, uses stable client IDs, enforces the facility hard limit of six, and plans to the operational target of five where capacity allows.
- Server-side assignment policy now blocks overcapacity, prohibited mixed-sex holding, routine placement into quarantine cages, and non-quarantine destinations for quarantine purchase intake.
- Animal cage movement uses receipts and animal expected versions; the scan/detail transfer UI submits stable command identities and blocks prohibited destination states.
- Missing/found is a reversible, audited, versioned command workflow. Missing animals leave their cage and open a critical lab-scoped alert; found animals require an authorized compatible cage and resolve the alert.
- Litter creation now uses an idempotent, expected-version breeding-setup command. The primary breeding worksheet routes weaning to the multi-cage intake planner.
- Routine mouse movement is same-lab only until the cross-lab request/accept/finalize state machine is implemented in Milestone 5; it never changes ownership or writes a direct lab-transfer record.
- Migration `0011_cage_capacity_hard_limit` audits and normalizes retained out-of-range values before enforcing facility/cage limits in PostgreSQL. Checksum: `1e11cf1ac541d13965e7b0569007f85a941b7e148dee3a8f0f28fabfbfc1bbe4`.
- Litter creation advances the breeding aggregate version in the same transaction. Retained PostgreSQL verification proves a stale second client is rejected without creating another litter.
- Focused TypeScript, ESLint, planner/capacity/assignment-rule/command tests, 122 safe regression tests, production build, migration replay/schema parity, retained command verification, and `git diff --check` are green. A post-browser-fix sweep adds 35 focused passing tests. The broad seeded colony suite is currently blocked before test execution because legacy fixture `alloc-004` crosses the ownership rules; this is retained as a fixture-reconciliation blocker rather than bypassing lab integrity.
- First Milestone 4 review returned NOT CLEAR with four P1 blockers: direct cross-lab movement, non-atomic draft commit, a configurable capacity above six, and litter creation without parent-version advancement. All four have been remediated; a second review is required before promotion.
- Second Milestone 4 review cleared three remediations and retained one P1 upgrade-safety blocker in migration `0011`. The migration now records append-only audit entries, normalizes legacy facility `7` and cage override `100` values to `6`, then validates constraints. The retained-upgrade verifier reproduces and passes this case. The reviewer also identified a P2 true-concurrency coverage gap; deterministic stale-review rejection and validation-failure restoration are retained tests, while a direct PostgreSQL race harness remains follow-up evidence.
- Third narrow review returned CLEAR with no P0/P1 blocker. Its P2 requests were closed with retained lower-bound normalization fixtures, exact before/after audit assertions, validated check-constraint metadata assertions, and administrator capacity tests for zero, fractional, and above-six values.
- Empty-instance browser QA at `1440x1100` and `390x844` verified login, full draft save/resume, immutable intake review, successful atomic empty-cage creation, cage-list retrieval, Animals/Breeding empty states, zero document overflow, and 44 px mobile intake controls. The isolated database contained exactly one cage, one charge period, one committed workflow draft, and one succeeded command receipt.
- Browser QA found and fixed two runtime-only Next 16 failures: the capability-driven `Users` navigation item had no icon, and a `"use server"` file exported an object. The icon registry is now compile-time complete and the action-state value lives in a neutral module. The reviewed command envelope now matches the immutable snapshot representation used by the command verifier.
- Final bounded review returned CLEAR with no P0/P1 blocker. Its P2 findings were closed by raising CSV/remove controls to 44 px and enforcing the server's 100-pup, 50-cage, and 300-animal batch limits in the client before review. Its remaining P3 request is targeted automated rendering/action-round-trip coverage; the browser/database proof above is retained while that coverage is added with the future E2E expansion.

### Milestone 5 evidence (in progress)

- The first slice adds an explicit breeding transition matrix for planned, active, paused, retired, and failed setups. Pause/resume/retire/fail actions require a date and reason and are exposed through the existing breeding worksheet row menu.
- Breeding transitions execute through `executeIdempotentCommand` with database actor reauthorization, `breeding_setup` expected-version protection, stable request identity, one serializable domain transaction, audit history, and command receipts.
- Creation and transitions now share a reauthorized idempotent command boundary in both the browser and v1 API. Breeder/cage reads, role checks, duplicate checks, and writes occur inside serializable transactions; concurrent creation produces one open setup and revoked actors are rejected.
- Resume blocks non-live parents, malformed sire/dam composition, split or invalid/quarantine cages, and active experiment assignments. Routine moves and terminal lifecycle removal cannot silently detach an adult from an open setup. Terminal transitions release parents and reconcile every stale breeding cage in the lab only when no other open setup still uses them.
- Effective dates are monotonic, cannot precede the latest litter, and cannot be in the future. Litter recording applies the same future-date boundary and revalidates live adults and the shared mating cage. Audit timestamps record transaction time while business-effective dates remain in event payloads.
- Browser-independent retained PostgreSQL verification proves future/backdated litter and transition rejection, pause/replay/stale resume/valid resume/terminal retirement, split-cage rejection, blocked breeder movement and lifecycle removal, concurrent create safety, actor revocation, parent release, cage reconciliation/audits, and terminal immutability.
- Migration `0012_active_ownership_trigger_dispatch` replaces a polymorphic trigger expression from migration `0009` that referenced fields absent from other trigger row types. Fresh replay, zero schema drift, valid update checks, and cross-lab rejection checks now pass for Animal, Experiment, Project, and BreedingSetup branches. Checksum: `14cacfb800433eda08ce39bf842c5be263b1895006ffa913514494c6847d2a19`.
- Desktop and mobile Chromium QA created a cage, four synthetic mice, and two breeding setups through the real UI; the latest setup used the reauthorized create command, the first setup paused and persisted after reload, desktop/mobile document overflow was zero, and the open mobile row menu had no visible control below 44 px.
- Prisma validation, TypeScript, ESLint, production build, focused state-machine/command/API privacy/seed-ownership tests, the retained command verifier, the expanded 12-migration verifier, and `git diff --check` pass. The destructive seed-backed 65-route suite was not rerun because the execution policy refused destructive database seeding; its pure ownership and API boundary subsets pass.
- Three bounded high-risk review rounds identified and closed chronology, cage reconciliation, lifecycle, concurrent creation, audit, and API-bypass findings. Final independent verdict: CLEAR, with no P0/P1 finding.
- Animal lifecycle disposition now uses a two-step details/review wizard and a reauthorized, expected-version, caller-key-idempotent command. Euthanasia, found-dead, external transfer, and archive transitions are date- and reason-bound; external transfer additionally requires durable destination provenance and an optional reference.
- The command transaction atomically removes the animal from its cage, records one movement and status event, ends open project allocations, updates the aggregate version, reconciles cage state, writes the lifecycle audit, commits the immutable review draft, and completes one receipt. Exact API retries replay the original result; conflicting key reuse returns `409` without a second write.
- Strict calendar validation rejects normalized nonexistent dates. Chronology includes DOB, status events, cage movements, and every open or completed project-allocation start while allowing legitimate same-day date-only operations. Open breeding relationships and planned/reserved/active experiment assignments block terminal disposition and archive until resolved.
- Terminal-but-unarchived animals are labeled `Not in cage`, not `Archived`. External transfer destination, reference, effective date, and reason are projected from immutable audit history to authorized animal detail and API reads without creating or conflating an `AnimalLabTransfer`.
- Retained PostgreSQL verification covers exact caller-boundary replay, conflicting-key rejection, stale versions, invalid dates, same-day movement chronology, open and completed allocation backdating, validation-draft restoration, archive relationship blocks, cage detachment, allocation cutoff, provenance, one-time status/audit effects, and actor revocation. The final retained schema was `mcm_test_command_foundation_1783908445253_41962`.
- Final verification: Prisma migration deployment across all 12 migrations, TypeScript, ESLint, 12 focused pure tests, production build, retained command verification, and independent high-risk review are green. Real Chrome QA at `1440x1100` and `390x844` proved UI commit, provenance after reload, API `200` first/`200` replay/`409` conflict, zero mobile overflow, 44 px controls, no browser errors, and exactly one movement/event/audit/receipt per UI or API disposition. Final reviewer verdict: CLEAR with no P0/P1/P2 finding.
- Quarantine now has an explicit admission, observation, exception-resolution, release-request, reviewed per-animal assignment, and release-finalization state machine. Case, observation, immutable review, version, command receipt, audit, alert, movement, and cage/animal changes commit through reauthorized serializable command boundaries.
- Release requires an active quarantine source cage, a latest `clear` or `exception_resolved` observation, no unresolved quarantine-generated follow-up, a committed review matching the current case version, authorized active destinations, and capacity/sex compatibility rechecked immediately before movement. Finalization is idempotent and atomically rolls back every assignment if any invariant changes.
- Reserved quarantine health-note provenance is shared by the case writer, ordinary cage-note writer, and v1 API validator. Routine writers reject spoofed provenance before dedupe; non-actionable clear/resolution observations close only current-case quarantine follow-ups and leave unrelated welfare notes untouched.
- Application and PostgreSQL containment guards block routine relocation, missing-state removal, cage status/lab changes, transfer, and exit while a case is open. Terminal detachment to no cage remains possible, while a coupled terminal outcome plus destination relocation is rejected. Observation inserts lock the case row and reject post-request history or implicit exception closure.
- Migration `0013_quarantine_case_state_machine` adds the constrained state/history model, append-only observation protection, observation serialization, and containment triggers. Its final checksum is `9ea0f63e0f1a37af257aaf0ac20fa59c851fda85712598e8de20f57140c3df43`; destructive history deletion additionally requires both the explicit seed flag and a disposable/test schema.
- Retained PostgreSQL command verification passed in `mcm_test_command_foundation_1783915922038_42033`, covering containment, provenance, follow-up lifecycle, release review/finalization/replay, receipts, outbox behavior, and migration exceptions. Fresh 13-migration replay, zero schema drift, and empty bootstrap passed in retained schema `mcm_empty_verify_1783915946840_42369`.
- Desktop and mobile browser QA completed the real admission-to-release flow, confirmed persisted movements/observations/review/receipts/alerts, zero mobile overflow, and 44 px controls. Evidence is retained at `/private/tmp/m5-quarantine-desktop-review.png`, `/private/tmp/m5-quarantine-mobile-review.png`, and `/private/tmp/m5-quarantine-desktop-final.png`.
- Final gate verification passes TypeScript, ESLint, 29 focused quarantine/seed/command/authorization tests, the 71-entry authorization manifest, production build, `git diff --check`, both retained-schema PostgreSQL verifiers, and an independent high-risk review. Final reviewer verdict: CLEAR with no P0/P1/P2 finding.
- Cage closure is now an explicit reviewed command backed by append-only `CageClosure` history and an immutable half-open billing cutoff. The review binds cage/lab identity, current aggregate version, the exact final charge period/category/start/rate/currency, date, reason, and every occupant destination; closed cages expose this final billing history in detail, scan, and cage-list views.
- Closure preflights all assigned animals and aggregate destination capacity/sex/quarantine/chronology before the first move. Any post-write divergence throws and rolls the serializable command transaction back. Same-lab ownership, open breeding, open quarantine, non-live assigned records, source/destination timestamps, stale cage versions, and draft/finalized invoice service beyond the cutoff block closure.
- Migration `0014_cage_closure_billing_cutoff` is transaction-wrapped, backfills coherent legacy closures, couples each closure to the exact cage/lab/charge-period tuple, binds database inserts to the immutable review payload and matching movements, prevents implicit reopen or later charge periods, and validates invoice finalization against current charge-period boundaries. Finalized and void invoice financial history is immutable outside the guarded disposable-schema seed path. Final checksum: `607555394bc3ed76285794a39c6acd3e503f5e507265b7e07f61869655387adc`.
- Second-pass hardening binds closure insertion to the reviewed aggregate version and active command receipt, proves every reviewed animal's final active same-lab destination, rejects direct reassignment into closed cages, and enforces that every open charge period follows its cage's current lab through deferred database constraints. Exact start-of-day chronology now returns a specific validation failure for same-day intraday activity.
- Invoice-line writes lock both affected invoices in deterministic order and validate draft, lab, period, cage, category, rate, currency, service bounds, day count, and amount before insertion or update. Reparenting from or into terminal invoices, terminal parent deletion, stale draft service after closure, and malformed pre-existing terminal invoices are blocked; finalization and void application paths also lock the invoice before review.
- The retained migration verifier now covers successful installation, early and late atomic rollback, multiple legacy open or overlapping charge periods, malformed and prematurely finalized terminal history, terminal invoice/line raw-SQL bypass attempts, closed-cage animal assignment, stale post-closure drafts, charge-lab drift, and lock-observed two-client finalization, closure-versus-charge-period, and closure-versus-movement races. Loopback access is approved, but the available Prisma Dev runtime is PGlite: its wrapper process terminates when the verifier deliberately raises the constraint errors needed to prove rollback and bypass rejection. These cases therefore remain runtime-unverified until a real PostgreSQL server is available locally or in CI.
- Charge-period writes now lock affected cages deterministically, reject overlapping service windows, and preserve every cage/lab/category/rate/currency/date fact referenced by finalized or void invoice lines. Application writes reject future invoice windows and give a controlled billing-history lock error before a rate or lab change would shorten protected service. Database finalization and populated-migration preflight both reject invoices finalized before their service period ends; revision-safe draft numbering and serialized lab-window checks prevent overlapping or colliding invoice regeneration.
- The final high-risk review returned CLEAR after five focused remediation rounds covering charge-period and movement races, overlapping invoice windows, exact-window regeneration, malformed draft voiding, terminal charge-history mutation, and legacy finalization chronology. No P0/P1/P2 finding remains in the reviewed M5-04 diff.
- The incompatible legacy `exitCage` mutation was removed. Destructive demo seeding now requires a disposable/test schema exactly like the PostgreSQL append-only escape hatch, and attachment cleanup occurs only after database reseeding succeeds.
- Disposable browser QA passed against `mcm_test_m5_qa` on `127.0.0.1:51430`. A Facility Admin created two empty cages through `/cages/intake`, reviewed and permanently closed both through the cage-detail UI, and retrieved the resulting closed state. The second flow proved the confirmation button disabled before acknowledgement and enabled after it. Readback confirmed `status=closed`, `active=false`, version `3`, one append-only closure, an exact matching charge-period cutoff at `2026-07-15T00:00:00.000Z`, one close audit event, and one succeeded `cage.close` receipt with resulting version `3`. Desktop and 390 x 844 mobile pages have no document overflow; cage action targets measure 44 px after removing a conflicting mobile override. Evidence is retained at `/private/tmp/m5-closure-desktop-final-20260715.png` and `/private/tmp/m5-closure-mobile-final-20260715.png`.
- Current gate evidence: Prisma schema validation, TypeScript, ESLint, 25 focused migration/billing/review-binding/UI tests, 71-entry authorization-manifest coverage, database-workflow policy checks, production browser QA, production build of all 47 routes, and `git diff --check` pass. The database-free projects pass 34 files and 162 tests, including permanent-closure acknowledgement, future-window billing, malformed-void, overlap, lock-order, and migration-preflight regressions. Fresh 14-migration replay and empty bootstrap pass in retained disposable schemas through `mcm_test_m5_guard5`. The test runner isolates seven destructive database integration suites behind `guard:test-db`; unmarked schemas are rejected before Vitest or Playwright can connect. Every seeded Playwright alias, including reuse-existing-server paths, uses the same fail-closed guard. CI uses independent `mcm_test_ci` and `mcm_e2e_ci` schemas and its workflow YAML parses locally. Build-time Google font downloads were removed in favor of a stable IBM Plex-first system stack, and `npm run build` uses the documented webpack path without network or a Turbopack listener.
- Final retained verification ran on signed and notarized Postgres.app PostgreSQL `16.14`, bound only to `127.0.0.1:51422`. It proved installation, early and late atomic rollback, multiple-open and overlap rejection, legacy cutoff rollback, malformed finalized/void and premature-finalization rejection, raw-SQL immutability guards, active-cage assignment constraints, and lock-observed finalization, closure-versus-charge-period, and closure-versus-movement races. Nine `mcm_verify_closure_*` schemas were retained without deletion. The temporary server was stopped, port `51422` was confirmed closed, and the disk image was unmounted.
- Real-PostgreSQL execution exposed and closed three verifier-only defects: cage/animal fixtures now reserve canonical facility IDs, the legacy cutoff fixture no longer accidentally overlaps, and lock observation follows a transitive FIFO blocker chain while still requiring the closure transaction as the root blocker. Post-verifier regression checks pass 34 files/162 tests, TypeScript, ESLint, and `git diff --check`. Final application review and verifier-harness review verdicts: CLEAR. M5-04 is Verified and M5-05 may begin.
- Cross-lab transfer now follows a durable three-party state machine: an active source-lab owner/manager creates or revises an immutable operational packet, an active destination owner/manager accepts the exact current packet and animal placement, and CMU or Facility Admin performs the atomic finalization. Ownership and cage billing remain unchanged until finalization; all transitions use database-reauthorized command receipts, expected aggregate versions, serializable transactions, and append-only request, packet, event, movement, and ownership histories.
- Animal and cage packets bind source/destination identity, requested effective date, animal/cage state fingerprints, destination placement, packet version, and SHA-256 payload hash. Any post-acceptance animal or destination-cage change produces `packet_stale` and requires source revision plus destination reapproval. Source-private notes never enter the destination packet, while source-lab reads redact destination-cage barcode, location, occupancy, capacity, sex counts, and fingerprint before serialization.
- Finalization re-locks the accepted request, immutable packet, animals, cages, relationships, charge periods, and destination capacity. Active experiment assignments, project allocations, and breeding participation block CMU finalization. Only Facility Admin may supply a durable override reason; the override cancels/ends/retirements atomically, reconciles affected animal and cage states, and records a distinct append-only override event and audit evidence.
- Migration `0015_cross_lab_transfer_state_machine` installs database command-context, principal, packet/hash, ownership, history, deferred commit-completeness, and append-only guards. Test-only destructive bypasses are bound to the firing table's `TG_TABLE_SCHEMA`, not spoofable `search_path`; request/item/packet/event/transfer/movement ledgers reject `TRUNCATE`; and trigger-authorized mutations require a receipt still in `processing`, preventing reuse of a completed command. Final checksum: `9fa2b249fc6f19d15dd1d7aba3b11c62914ec6fb50d1d5d88e82355abbfa5114`.
- Closure-driven batched moves defer per-animal cage maintenance only inside the caller transaction, then update and reconcile every destination cage after all occupants move. The source cage remains bound to its reviewed version and is advanced exactly once by permanent closure. A real PostgreSQL regression proves a stale destination `breeding` cage becomes `active` with an audit event while the closure remains atomic.
- Final retained PostgreSQL verification on `127.0.0.1:51422` replayed all 15 migrations with zero schema drift, bootstrapped the empty instance, rejected raw ownership/history/hash/receipt/TRUNCATE bypasses, exercised packet staleness and reapproval, proved CMU block and Facility-only override behavior, and completed both cage and animal transfers with billing/history boundaries. All 72 authorization entry points, 178 unit tests, 138 database tests, TypeScript, ESLint, Prisma validation, `git diff --check`, and the production build of 47 routes pass. Independent high-risk re-review verdict: CLEAR with no P0/P1/P2 findings. M5-05 through M5-07 are Verified.
- SOP governance now provides facility and private-lab documents, immutable database-hashed versions, independent approval, current-approved exact-version assignment, one-time revocation, explicit exact-content acknowledgement, role-projected draft/roster privacy, and state-aware assignment delivery. Lab creation is bound to the selected active lab; PostgreSQL stamps command receipts with current `authzVersion` and `SESSION_USER`, protects that evidence, and revokes public access to every privileged SOP function.
- Fresh PostgreSQL schema `mcm_test_sop_hardened_20260716_r12` replayed all 16 migrations successfully. Read-only catalog checks prove both principal-evidence columns and receipt triggers are installed, all six SOP functions are security-definer with `PUBLIC` execution disabled, and Prisma reports zero schema drift. TypeScript, ESLint, 192 unit tests, the 74-entry authorization manifest, schema validation, workflow-policy checks, `git diff --check`, and the production build of all 48 routes pass. The earlier `r10` forged-write integration passed before the final receipt-evidence extension; after the Mac restart, tenant execution policy denied the latest data-writing integration rerun and empty-instance seed, so browser acceptance remains explicitly pending rather than inferred. Independent security re-review verdict: PASS with no P0/P1/P2 finding under the documented trusted-backend threat boundary.
- The research workspace slice adds versioned experiment registry creation, same-lab project binding, planned/active/completed/cancelled lifecycle commands, operational scheduling/welfare fields, private lab notes/results, and a CMU operational projection that never selects the private columns. Mutations require `experiments:manage` rather than private-field read access, reauthorize active membership in the command transaction, use idempotent receipts and expected versions, and write domain audits. Lab viewers retain full lab-private read access without mutation controls.
- Biosamples retain immutable source-lab ownership when an animal later transfers. Optional experiment linkage is enforced with composite `(experimentId, labId)` and project linkage with `(projectId, labId)` foreign keys; app, API, inline worksheet, and workbook projections agree on that historical policy. Biosample lifecycle/storage rules, terminal states, expected-version updates, atomic duplicate-label handling, and same-project experiment checks are enforced server-side. CMU has neither Biosamples navigation/API capability nor a Workbook Biosamples sheet.
- Fresh PostgreSQL schema `mcm_test_research_20260716_r13` replayed all 17 migrations and reports zero Prisma drift. Read-only catalog checks confirm the experiment date check, biosample version check, and both same-lab composite foreign keys. TypeScript, ESLint, 34 focused pure tests, the 74-entry authorization manifest, Prisma validation, workflow-policy checks, and `git diff --check` pass. Destructive seeded integration and browser acceptance remain policy-blocked and are not inferred from the migration proof. Independent high-risk review is pending.
- Research-service remediation migrated every experiment-assignment mutation, including direct animal reservation, onto database-reauthorized idempotent commands with exact experiment, assignment, and animal versions. Cross-experiment reservation races fail closed, transfer overrides advance and audit parent experiments plus assignments consistently, CMU operational DTOs omit private-capable keys entirely, and biosample duplicate replay now requires an exact normalized inventory and provenance snapshot without returning foreign historical IDs. Fresh schema `mcm_test_research_20260716_r14` replayed all 18 migrations with zero Prisma drift. The final database-free gate passes 50 files and 244 tests, TypeScript, ESLint, the 74-entry authorization manifest, Prisma validation, workflow-policy checks, the 48-route production build, and `git diff --check`. Two review-found P2 form-path issues were closed with capability-derived UI gating and transaction-scoped active-lab experiment lookup; narrow re-review verdict: CLEAR with no P0/P1/P2 finding. Destructive seeded integration and browser acceptance remain tenant-policy blocked and are not inferred.

### Milestone 6 forecast evidence

- Added durable many-to-many cage responsibility history with exact cage/lab/member identity, assigned and ended actors, reasons, timestamps, and versions. Active rows require an active operational member in the cage's current lab and are unique per cage/user; history is immutable except for one command-authorized close.
- Responsibility changes use a database-reauthorized, idempotent, expected-version `cage.responsibility.update` command and atomically advance the cage plus write before/after audit history. Cage transfer and closure end active responsibility rows before changing lab or operational state. User or membership deactivation is blocked until active cage responsibilities are reassigned.
- Cage detail and scan views expose the same capability-gated assignment control and current responsible-user summary. The legacy role-name UI gate was replaced with the central `cages:manage` capability, preserving owner/manager/staff access after the role migration.
- Forecast reads now require an explicit current `ResolvedActor`; the implicit global actor was removed from every export. URL-backed lab, responsible-user, and cage filters resolve only against accessible active cages, and validated scope is applied server-side to breeding parents, available animals, experiment demand, and inventory counts. Multi-lab forecast rows carry lab, cage, and responsible-user attribution.
- Migration `0020_cage_user_forecast_scope` replayed with all prior migrations in fresh retained schema `mcm_test_forecast_20260716_r21` on PostgreSQL 16 at `127.0.0.1:51422`; read-only catalog checks found two assignment triggers, seven indexes, eighteen constraints, and principal guards on User, Lab, and LabMembership. Checksum: `f6b626345dcf2cdb1bd216cf21a84fcbddfbbad00ca9a1cdd6e02c2df50792e6`.
- Current gate evidence: Prisma validation and generation, TypeScript, ESLint, 58 non-destructive test files with 272 tests, 9 focused assignment/forecast tests, the 77-entry authorization manifest, the 49-route production build, and `git diff --check` pass. Destructive seeded integration and browser acceptance remain explicitly pending under the existing tenant execution restriction.
- The post-restart subagent review runtime accepted both high-risk and bounded fast review jobs but returned no result before shutdown; the main-agent gate rechecked the migration receipt guards, command authorization/version boundary, transfer/closure cleanup, principal-deactivation guards, forecast query scope, UI capability gates, and focused tests and found no blocking issue. This reviewer-runtime limitation is recorded rather than presenting an independent verdict that was not received.

### Milestone 6 cryostorage evidence

- Added lab-owned, versioned cryostorage requests for storage, recovery, and discard; immutable request events; and one immutable execution record per completed request. Lab staff submit and cancel through `cryostorage:request`; CMU/Facility actors complete or reject through `cryostorage:manage`; every command reauthorizes the current database actor and lab before mutation.
- Submission, cancellation, and execution are serializable, idempotent commands bound to current command receipts, exact request versions, current actor authorization versions, database principals, labs, and aggregates. Recovery/discard execution also requires the target inventory version captured at submission, so intervening metadata or status changes fail closed without writing an operation.
- Same-lab composite foreign keys bind requests, projects, target records, operations, and inventory. Database guards reject direct request transitions, keep event/operation history append-only, require exactly one operation for a completed request, and require exactly one submitted event plus one matching terminal event. Direct CMU inventory API compatibility remains capability-gated and optimistic-versioned; the request workflow is the primary lab-facing operation path.
- PostgreSQL 16 fresh schema `mcm_test_cryo_20260716_r26` replayed all 22 migrations and reports zero Prisma drift. Catalog checks found three custom triggers on requests, two on events, three on operations, and the unique request/event-type index. Retained verification proves authorized submission, raw-SQL submission rejection, missing-event commit rejection, CMU storage execution, stale target rejection with no operation, request rejection, cancellation, and complete event/audit history.
- Migration checksums: `0021_cryostorage_requests` is `4ce9341f3351e3beb2e3a45b225fac0c8c160ec181943fbc0a3c97d498053d83`; `0022_cryostorage_event_integrity` is `d3ee9706d9b7d2cc312b33cb78dffdfde9752ba3d77e07e0c164c03b5a26cfc3`.
- Final gate evidence: 62 non-destructive test files with 284 tests, TypeScript, ESLint, Prisma validation/generation, the 77-entry authorization manifest, the 49-route production build, and `git diff --check` pass. A concurrent TypeScript/build invocation briefly observed build-generated `.next/types` files being replaced; the required isolated TypeScript rerun passed after the build completed.
- The independent high-risk reviewer runtime remained running without returning a result through two bounded waits and was shut down. The fallback main-agent review found the missing exact event-parity invariant, added forward-only migration `0022`, and reran the full gate and retained verifier successfully. Browser acceptance remains pending rather than inferred from database and build evidence.

### Milestone 7 notification evidence

- Notifications now separate immutable events, explicit audiences, exact per-user recipient generations, personal preferences, and durable email deliveries. Event identity includes the owning lab, only one active recipient generation is permitted, and authorization changes revoke the old generation before a new row is materialized, preserving prior audience and revocation provenance.
- Urgent warning/critical welfare notes materialize and queue exact-recipient email jobs in the same health-note transaction. Routine categories remain immediately available in-app and enter daily or weekly email only after that individual opts in. Preference changes cancel obsolete digest jobs and bind replacements to the new preference version.
- Notification email uses the existing transactional outbox with authenticated topic-bounded workers, leases, retries, dead letters, and technical attempts. Notification jobs are institutional obligations: claim and completion revalidate the exact active recipient, current role/authz version, lab membership or facility role, event, preference version, target email, and outbox payload. Provider calls hold the entity/recipient authorization locks across a bounded send, use the immutable delivery ID as the provider idempotency key, reject redirects, accept only explicit HTTPS hosts in production, reject non-global IPv4/IPv6 answers, and pin the socket to the validated address while preserving the allowlisted TLS hostname. Provider URL, sender, host allowlist, token, and worker token are deployment-controlled; arbitrary rule-configured URLs and global recipient lists are no longer used for delivery.
- Read-time inbox assembly re-resolves the current database user and active-lab membership in a serializable transaction, authorizes the exact recipient audience rather than any event audience, and durably revokes stale rows. Cross-lab finalization resolves source-lab animal/cage notifications and cancels queued source-recipient jobs before ownership changes complete.
- Independent review found and closed lab-transfer event identity, stale membership reads, recipient provenance overwrite, duplicate delivery generations after read/ack actions, provider crash/retry idempotency, ownership races during provider send, stale source-alert reopening, unguarded verifier writes, configurable-URL SSRF/token exfiltration, DNS rebinding/non-global address handling, and a missing authorization-manifest action. Final narrow high-risk verdict: `CLEAR`, with no P0/P1/P2 finding.
- Fresh retained schema `mcm_test_notification_hardened_20260717_r32` replayed all 25 migrations on PostgreSQL 16 at `127.0.0.1:51422` and the datasource-aware Prisma comparison reports zero drift. Its single clean retained-verifier run proves owning-lab and facility recipients (including facility actors who also hold lab memberships), foreign-lab exclusion, lab-bound source identity, read/stale/cross-user rejection, durable revocation, immutable recipient generations, no duplicate delivery after read/rematerialization, urgent exact-recipient completion, routine digest opt-in/cancellation, and event/audience/recipient/preference/delivery history guards.
- Migration checksums: `0023_notification_recipients` is `261d0b0f18e703c890d51a500332f9e30bb1f2d7fd82652b0ea6955d6a3372c7`; `0024_notification_delivery` is `c256422afd7694deedcaf3f32287f2dcebf8deac71e10cb948c9f7a3a26b764d`; `0025_notification_delivery_hardening` is `ab669697512b84b56ebe1ae54c559a826e4ed2dc932bfd262dc9a530ded08532`. Current gate evidence: all 67 database-free test files and 310 tests, Prisma validation/generation, TypeScript, ESLint, the 78-entry authorization manifest, retained PostgreSQL verification, zero drift, the 49-route production build, and `git diff --check` pass. The independent sandbox declined the guarded destructive seeded DB project despite its disposable target, so that suite is recorded as not run rather than inferred. Notification UI browser acceptance is batched with the complete Milestone 7 role-interface pass.

### Milestone 7 billing evidence

- Migration `0026_invoice_governance` adds serialized invoice numbering, draft/finalized/void lifecycle guards, append-only adjustments, immutable terminal line snapshots, exact charge-period linkage, and optimistic versions for cage-rate categories. Its populated-history preflight locks invoice, line, charge-period, and category writers before validating positive whole-day UTC period boundaries, supported billing currencies, and terminal history. Final checksum: `5e2ea5a4f1b508f67b93829614b8c7f52ec092ada519a2a86575378bdfdeb12b`.
- Charge-category changes, intake enrollment, and cross-lab charge boundaries are effective only at UTC day boundaries. Application commands accept strict `YYYY-MM-DD` values, replacement locks the active period and rejects fractional or same-day replacements, and a database insert/update trigger rejects non-midnight history. Billing calculations use half-open UTC service days and immutable category name/code snapshots; terminal invoice display does not consult renamed live categories.
- Currency input is normalized against one explicit 162-code ISO currency set at both action and write boundaries. Migration preflight and database constraints use the same list, with a parity regression preventing drift. Legacy invalid values use a non-throwing explicit-code fallback instead of crashing a billing page or being mislabeled as USD.
- Finalization and voiding are actor-aware idempotent commands. Final numbers are assigned only at finalization; append-only debit/credit adjustments preserve subtotal provenance. Finalization notifies every active owning-lab member, excludes foreign and inactive members, and preserves recipient history through later void events.
- Fresh retained PostgreSQL 16 schema `mcm_test_billing_governance_20260716_r42` replayed all 26 migrations from empty without a seed/reset and reports the migration chain up to date. Its additive rerunnable verifier bootstraps only minimal test configuration and proves a two-rate service period (`2 x $2.50 + 2 x $3.00 = $11.00`), net `$1.00` adjustments, `$12.00` final total, lock-observed migration writer blocking, UTC rate boundaries, raw fractional and zero-length period rejection, command and raw-database `ZZZ` rejection, rate-receipt versions `1 -> 2`, stale-rate rejection, terminal category snapshot protection, append-only charge/adjustment history, foreign-lab finalization rejection, active-member notification fanout, and inactive-member exclusion. The shared command verifier also passed in retained schema `mcm_test_command_foundation_1784251228552_53736`.
- Current code gate: 71 unit files and 341 tests, Prisma format/validate/generate, TypeScript, ESLint, the 49-route production build, and `git diff --check` pass. The guarded destructive database integration invocation was policy-rejected and is not inferred; strict intake parsing has a database-free regression and the dedicated retained verifier proves the database trigger. Named-schema `migrate diff` emits namespace churn (`public` versus retained `mcm_test_*`), so schema confidence is based on fresh full migration replay plus `prisma migrate status`, not a false zero-diff claim.
- Browser acceptance on the retained r37 schema passes at desktop and 390 x 844 mobile widths with zero document overflow. Facility Admin sees invoice generation and rate maintenance; Lab user 2 sees only Lab 2 cage charges/rates, no billing mutation controls, and receives 404 for the direct Lab 1 invoice URL. The terminal invoice renders frozen line descriptions, exact service/rate periods, append-only adjustments, and totals on both breakpoints. A stitched full-page mobile capture distorted sticky content, but normal viewport captures and measured element widths were correct.
- Independent high-risk review found and closed fractional and zero-length period persistence, unsupported currency storage/rendering, missing rate-receipt resulting versions, stale rate edits, writer-lock ordering, recent-list total truncation, mixed-currency aggregation, and terminal category snapshot drift. Final narrow verdict: `CLEAR`, with no P0/P1/P2 finding. M7-04 through M7-06 are Verified.

### 2026-07-17 — Audit And Security Separation (review gate in progress)

- Split operational `AuditLog` history from a fixed-field `SecurityEvent` ledger. Identity governance, authentication, API authorization, and outbox security events use the technical ledger; Facility Admin receives domain metadata without before/after JSON or identity events, while IT receives only security/runtime projections without scientific records, outbox payloads, or migration error text.
- Hardened provenance at the database boundary. New operational audits receive a canonical actor role and bind command-backed lab/request context to a receipt owned by the same actor; caller conflicts are rejected. Legacy identity events are copied with generic summaries and `legacy_unsnapshotted` role provenance only when their ID, actor, entity, and timestamp match the retained identity audit.
- Bounded attacker-controlled telemetry with five-minute keys, an in-process 2,048-key cap, database uniqueness, `createMany(skipDuplicates)`, a 250 ms best-effort timeout, and a 30-second circuit breaker. Mandatory identity/outbox events have stable replay-safe dedupe keys.
- Replaced capped history with cursor pagination, search, role-appropriate filters, and capability-guarded CSV exports. Settings exposes operational action/entity/actor/lab/request metadata; System exposes security severity/outcome/event/subject metadata.
- Enforced the notification provider contract with `NOTIFICATION_EMAIL_PROVIDER_SUPPORTS_IDEMPOTENCY=true`. The request builder derives both header and body keys from the durable delivery ID, and a database-free ambiguous-acceptance retry regression proves byte-identical request identity.
- Staged migration deployment across `0027` through `0032`: short additive DDL with bounded lock/statement timeouts, separate legacy backfill and constraint validation, and one single-statement concurrent-index migration per audit index. Checksums: `0027` `3f9df9d9716b87ae0748810bced6dd63df4cd5367288c113fb31d21f0c2afea2`; `0028` `a2b379205e9d67fe4829457f939c40c06ed6f15e5c85ba0200e5649737613de9`; `0029` `5b2bc7a9cda9ba38e5cc94ca18322c044c16d6471c58017166299193ecd32940`; `0030` `3cd71165765851fbf327ded3ef16f320b7268755d24e4d28c72aae401e036f4d`; `0031` `6af099b855ce86c9b9be38c68c5cdd2939ccf9ed9fdc11f6486dc5ab74c383d3`; `0032` `38e9105b49b71fcfb94ed45941f127c9ca4362f2a24da3d23ad9ecf48c326ac7`.
- Fresh PostgreSQL 16 replay passed all 32 migrations in retained `mcm_test_audit_security_20260717_r52`. Populated rehearsal cloned untouched migration-26 r42 into retained r53, added synthetic legacy identity/domain rows, and applied `0027`-`0032`: 32 migrations recorded, all five audit indexes valid/ready, zero unvalidated constraints, generic identity security copy with no sensitive text, `legacy_unsnapshotted` actor role, and no domain row copied.
- The retained-target guard runs before verifier client construction. Non-destructive r52 verification passed canonical stamping, trusted receipt/lab binding, malformed/spoof insert rejection, role projection separation, and no payload leakage. Host execution policy refused the UPDATE/DELETE/TRUNCATE and seeded delivery fixture commands even in approved disposable schemas; those runs are not claimed. Earlier retained r46 proved the same append-only trigger bodies, while current tests assert exact `55000`/`23514` handling and unchanged-row checks.
- Current gate: Prisma generate/validate, TypeScript, ESLint, 74 database-free files / 359 tests, 78 authorization entry points, database-workflow guard, production build of 49 routes, and `git diff --check` pass. Independent high-risk re-review is active; M7-07/M7-08 remain In progress until it returns CLEAR.

### 2026-07-17 — Audit And Security Review Remediation

- Closed every blocker from the first independent M7-07/M7-08 review. Retained verification now parses and compares the semantic database target from both `DATABASE_URL` and `DIRECT_URL`, requires loopback PostgreSQL plus an `mcm_test_*` schema, constructs Prisma with the guarded datasource URL, and confirms `current_database()` and `current_schema()` before any write probe.
- Removed caller-asserted provenance from both ledgers. Operational audit lab/request context is accepted only when a command receipt owned by the same actor proves it; explicit unreceipted context is rejected and JSON payloads are never mined for provenance. Technical events expose non-authoritative `scopeLabId` and `correlationId` fields rather than presenting them as canonical lab/request provenance.
- Superseding the earlier notification-delivery implementation note: provider network I/O now occurs outside database transactions. A short serializable prepare transaction persists immutable provider bytes, SHA-256 hash, recipient, durable idempotency key, and exact notification event version; a short finalize transaction records the provider result. Ambiguous retries reuse the stored bytes and stable key, and an authority change during the external call is recorded as a critical security event while preserving the truthful external delivery outcome.
- Hardened history exports against omission and spreadsheet injection. Audit/security exports require an explicit maximum 31-day range, pre-count and reject more than 50,000 records with HTTP 413 instead of silently truncating, and quote string cells whose first non-control/space character is `=`, `+`, `-`, or `@`; numeric cells remain numeric.
- Made concurrent-index interruption diagnosable and recoverable. Migrations `0029`-`0032` each contain one unguarded `CREATE INDEX CONCURRENTLY`; `check:audit-index-health` verifies every required index is present, valid, and ready. `docs/DATABASE_RECOVERY.md` documents exact invalid-index inspection, concurrent drop, migration resolution, redeploy, and recheck steps and forbids marking a missing/invalid migration applied.
- Current migration checksums:
  - `0027_audit_security_separation`: `94acb5f148a09531ee6f24dddd8792cbf52270c33667a7394cbb4fbbbf16901a`
  - `0028_audit_security_backfill`: `ee7f4357ac07a2e82d48c79bfd3d63b41354eaa7f9254c7aa32bb0dd7112aedc`
  - `0029_audit_timestamp_index`: `f2e3ffd1a1d07008bfbc1723d3110ed4f6c6bf891cc35deaf8e57000fd4c26be`
  - `0030_audit_lab_timestamp_index`: `8fb24e8e507b7901446affc1f5cf113f1fc6300121ed11d2a040aacb2c3201b4`
  - `0031_audit_entity_timestamp_index`: `ff0596b499c01bd93b4039df4fcf5cd4ac1db63ba807812070a6b8f3a8149750`
  - `0032_audit_request_index`: `5cac12e560635a9a34bf201b847a24ad3171570e2839ef70e5b4c6ff0e52e51d`
  - `0033_notification_provider_snapshot`: `6d1a3bfd76a9fe0740ceb1f81f36e5752fdb215d53472d5fc742f81d31930207`
- Fresh retained PostgreSQL 16 schema `mcm_test_audit_security_20260717_r60` replayed all 33 migrations, has zero Prisma schema diff, and reports all four audit indexes valid/ready. Its guarded non-destructive verifier passed canonical actor-role stamping, receipt-derived lab/request provenance, role-separated projections, and exact target confirmation.
- Populated upgrade rehearsal cloned the untouched 26-migration retained source `mcm_test_billing_governance_20260716_r42` into `mcm_test_audit_upgrade_20260717_r61`, inserted synthetic legacy identity/domain audits and a queued pre-snapshot notification delivery, then applied migrations `0027`-`0033`. It finished with 33 migrations, zero invalid indexes, zero unvalidated constraints, one generic `legacy_unsnapshotted` identity security copy with no sensitive payload, no copied domain audit, and explicitly cancelled the unsafe legacy queued delivery/outbox. The source remained at 26 migrations; r61 also has zero Prisma schema diff and passes the guarded verifier and index-health check.
- Final local gate passes: Prisma generate/validate, TypeScript, quiet ESLint, 76 unit files / 377 tests, 78-entry authorization manifest, database-workflow guard, 49-route production build, and `git diff --check`. Host execution policy still refused the guarded seeded integration suite and destructive UPDATE/DELETE/TRUNCATE probes even in approved disposable schemas; those executions are not claimed and were not retried or bypassed. M7-07/M7-08 remain In progress until a fresh independent high-risk re-review returns CLEAR.

### 2026-07-17 — Second Audit/Security Review Remediation

- The second independent review returned `NOT CLEAR` with one P1 and six P2 findings. All seven are remediated; M7-07/M7-08 remain In progress until a new independent reviewer returns `CLEAR`.
- Replaced JSON-derived receipt claims with a dedicated `AuditLog.commandReceiptId` foreign-key relation. `executeIdempotentCommand` establishes transaction-local audit context only after authorization/version checks and immediately before the handler. The database stamps the receipt relation, external request ID, lab, command type, and command aggregate from a receipt that matches actor, command type, request hash, processing state, and the same immutable PostgreSQL transaction ID. Command-receipt audit identity is database-stamped and immutable. Old, failed, wrong-command, wrong-hash, cross-lab same-actor, and unreceipted context probes are rejected; audit `previousValue`/`newValue` JSON is never inspected for provenance.
- Retained verification now requires both `DATABASE_URL` and `DIRECT_DATABASE_URL`, independently validates each loopback `mcm_test_*` target, requires semantic equality, constructs Prisma with the guarded runtime URL, and verifies the live database/schema before writes.
- Best-effort security telemetry now runs with a PostgreSQL-side 200 ms `statement_timeout`, bounded connection acquisition/transaction lifetime, a 250 ms caller budget, and an in-flight gate that remains closed until the underlying transaction settles. A caller timeout therefore cannot accumulate detached shared-pool writes.
- Immutable notification provider snapshots now include adapter, normalized endpoint, and non-secret account identity in addition to exact bytes/hash/recipient/idempotency/event version. A changed provider identity dead-letters the job, completes the attempt/delivery as failed, writes a critical technical event, and performs no network send until operator reconciliation. Provider I/O remains outside database transactions.
- Migration `0033` now closes every processing attempt for an affected legacy queued delivery as failed with `completedAt` and the migration cancellation reason before cancelling the outbox/delivery.
- Audit/security exports now perform one deterministic `take: 50_001` read and reject the extra row. Exactly 50,000 rows export; concurrent inserts cannot exceed the declared response/memory bound through a count/read race.
- Audit index health now reports schema, table, full `pg_get_indexdef`, expected and recorded SHA-256 checksums, validity/readiness, and Prisma completion/rollback state. Recovery has separate documented branches for invalid/mismatched indexes and for a valid exact index with unfinished Prisma metadata; no automatic resolve or destructive action is performed.
- Technical `scopeLabId` and `correlationId` are labeled as reported context in the IT UI. Mandatory security-event dedupe collisions now compare immutable event semantics and fail rather than silently returning a different event.
- Current migration checksums:
  - `0027_audit_security_separation`: `7702be109a1792ef72cce3f6f37f4df045275a9776f7ed473e7940287a586b2d`
  - `0028_audit_security_backfill`: `0698ef9c177ce92cf3c22caf6b4a0a29ec874cdccfe1bffa37f2cc7296d1b905`
  - `0029_audit_timestamp_index`: `f2e3ffd1a1d07008bfbc1723d3110ed4f6c6bf891cc35deaf8e57000fd4c26be`
  - `0030_audit_lab_timestamp_index`: `8fb24e8e507b7901446affc1f5cf113f1fc6300121ed11d2a040aacb2c3201b4`
  - `0031_audit_entity_timestamp_index`: `ff0596b499c01bd93b4039df4fcf5cd4ac1db63ba807812070a6b8f3a8149750`
  - `0032_audit_request_index`: `5cac12e560635a9a34bf201b847a24ad3171570e2839ef70e5b4c6ff0e52e51d`
  - `0033_notification_provider_snapshot`: `3e6662ca39a784fd92d2f127ea55e1d97e1392fa06573c0084a618e191b44866`
- Fresh retained PostgreSQL 16 schema `mcm_test_audit_security_20260717_r62` replayed all 33 migrations, reports zero Prisma schema diff, and has four healthy audit indexes with exact definitions/checksums. Its guarded verifier passed canonical actor/command/request/lab/aggregate stamping and all additive spoof/graft probes.
- Populated upgrade rehearsal `mcm_test_audit_upgrade_20260717_r63` cloned the untouched 26-migration source `mcm_test_billing_governance_20260716_r42`, added synthetic sensitive identity/domain audits plus a leased queued delivery with a processing attempt, then applied migrations `0027`-`0033`. It finished at 33 migrations with zero drift, invalid indexes, or unvalidated constraints; created one generic `legacy_unsnapshotted` identity event with no sensitive text; copied no domain audit; cancelled the unsafe legacy delivery/outbox; and completed the processing attempt as failed with the exact migration reason. The source remains at 26 migrations. r63 passes exact index health and the guarded verifier (`1/1` legacy identity backfill).
- Additive retained interruption fixture `mcm_test_audit_index_recovery_20260717_r64` contains four valid exact indexes but an unfinished `0032` Prisma row. The checker fails closed and identifies only that row as `eligible_resolve_applied_after_operator_verification`, proving the non-destructive recovery branch without updating migration metadata.
- Final local gate passes: Prisma validate/generate, TypeScript, quiet ESLint, 76 unit files / 382 tests, 78-entry authorization manifest, database-workflow guard, 49-route production build, and `git diff --check`. The guarded seeded integration suite and destructive UPDATE/DELETE/TRUNCATE probes remain execution-policy limited and are not claimed or bypassed.
- Fresh independent high-risk re-review verdict: `CLEAR`, with no P0/P1/P2 finding. The reviewer independently reran 8 focused files / 69 tests, TypeScript, scoped diff checks, and matched all seven current migration checksums. M7-07 and M7-08 are Verified; M7-09 may begin.
- Tracked non-blocking P3 hardening: command-receipt aggregate identity can be populated after an early create-command audit. The receipt relation and immutable audit snapshot remain valid, and the reviewer found no realistic lower-privileged attack path. A forward-only hardening should populate aggregate identity before the first command audit and forbid later changes once set or terminal.

### Milestone 7 administration evidence

- Added Facility Admin lab creation, profile/status management, and membership assignment/status management at `/administration/labs`. Lab-owner access is limited to the exact active lab profile; membership and lab-state authority remains Facility Admin-only. Every write re-reads the actor's active state, canonical role, and authorization version inside a serializable transaction. Membership and lab-state changes increment affected users' authorization versions, record fixed-field security history, and retain supplied reasons.
- Lab deactivation fails closed while active cages, current animals, open charge periods, draft invoices, open breeding setups, active/planned experiments, open quarantine cases, or open cross-lab transfers remain. Deactivation revokes pending lab invitations, records each revocation, and invalidates active member sessions.
- Invitation governance now supports explicit revocation and token-rotating resend. All pending predecessors for the canonical email are revoked and recorded before a replacement token is issued; accepted invitations, inactive labs, existing accounts, stale administrators, and concurrent changes fail closed. The Users worksheet derives expired display state from the deadline rather than offering an invalid revocation action.
- Privileged role decisions are consolidated at `/approvals`. Request creation and approval both re-read the live target account inside their serializable transaction. Approval requires a different current Facility Admin, rejects inactive targets, revalidates that the transition still touches IT Head or Facility Admin, and revalidates an active lab membership before a Lab User transition. Rejection requires an independent administrator and a recorded reason.
- Concurrent invitation creation/revocation/acceptance, privileged decisions, routine role changes, and account-state changes now convert Prisma uniqueness, missing-row, and serialization conflicts into stable fail-closed refresh responses. Identity security events retain administrator reasons and before/after role context rather than discarding validated intent.
- The IT-only `/system` worksheet adds cursor-paginated outbox visibility with status/topic/ID filters and a deliberately narrow projection. It never selects message payloads, provider data, aggregate identifiers, last-error text, or attempt error messages. Facility and lab roles fail the dual `system:view` plus `audit:security` capability guard before any queue query.
- Navigation, route capability mapping, and the machine authorization manifest include lab administration. The manifest currently covers 80 entry points.
- Current database-free gate passes: 81 test files / 402 tests, TypeScript, quiet ESLint, the 80-entry authorization manifest, database-workflow guard, production build of all 50 routes, and `git diff --check`.
- After the second Mac restart, the disposable PostgreSQL cluster restarted cleanly but the most recent administration schema was not present. Fresh retained schema `mcm_test_administration_20260717_r66` was created and successfully replayed all 33 forward-only migrations on PostgreSQL 16. Tenant execution policy still rejected the destructive empty-profile bootstrap before it ran, despite the approved, empty `mcm_test_*` target. The command was not retried or bypassed. Authenticated browser persona acceptance and seed-dependent administration integration therefore remain explicitly unverified for this gate.
- The first fresh M7-09 review returned `NOT CLEAR` with three P2 findings and one P3: privileged account state changes bypassed independent approval, pending grants did not bind requester authority, membership history omitted before/after authority, and concurrent first membership assignment could surface `P2002`. All were remediated. Privileged activation/deactivation now uses the same independent decision queue, request creation snapshots the requester's authorization version, approval and rejection revalidate that requester as an active current Facility Admin, and migration `0034_privileged_account_state_approval` expires every legacy pending decision before requiring the snapshot. Membership events retain reconstructable before/after role and active state plus the complete accepted reason, and `P2002`/`P2034` produce stable refresh responses.
- Fresh independent high-risk re-review verdict: `CLEAR`, with no P0/P1/P2 finding. A follow-up review also returned `CLEAR` after the remaining P3 rejection bookkeeping was hardened. The current gate passes 82 database-free files / 408 tests, TypeScript, ESLint with five non-blocking pre-existing unused-parameter warnings, 80 authorization entry points, database-workflow policy checks, a production build of all 50 routes, and `git diff --check`.
- Retained PostgreSQL 16 schema `mcm_test_administration_20260717_r66` contains all 34 forward-only migrations and has zero Prisma schema drift. Tenant execution policy rejected the agent-run empty-profile bootstrap before execution; the command was never bypassed.
- The user completed the guarded empty-profile bootstrap in retained r66. Readback confirms five active personas, two active labs, two active manager memberships, zero operational colony data, and all 34 migrations. Authenticated browser acceptance passed with the shared profile switcher: Facility Admin can open labs, users, and consolidated approvals; IT Head sees the technical console and is denied lab administration; Facility Admin is denied the technical console; Lab 1 manager is denied facility administration; CMU Staff receives facility operations without administration links. `/administration/labs`, `/approvals`, and `/system` pass at 390 x 844 with zero document overflow and visible controls measuring at least 44 px. M7-09 is Verified.

## Open Blockers And Decisions

- Production authentication provider and high-privilege MFA are required before production, but do not block empty-instance development.
- Exact retention periods, billing numbering/rounding, and SOP acknowledgement categories must be finalized before their respective milestone exits. Urgent email is finalized as warning/critical welfare only; routine categories are per-user digest opt-ins.
- The populated application remains blocked until all P0/P1 authorization and migration requirements are verified.
- The synthetic `alloc-004`/sample ownership mismatch was reconciled and pure seed-ownership tests pass; the broad destructive-seed route suite still requires an execution environment that permits its guarded reset.
